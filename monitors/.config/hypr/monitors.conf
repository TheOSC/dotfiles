# =============================================================================
# monitors.conf
# Generated and managed by hyprdynamicmonitors
# DO NOT EDIT MANUALLY — use: hyprdynamicmonitors tui
#
# Initial safe fallback — hyprdynamicmonitors overwrites this at runtime
# based on detected display configuration and saved profiles
# =============================================================================

# Laptop internal display — preferred resolution, auto position, scale 1
# hyprdynamicmonitors will replace this with your actual configured profiles
monitor = eDP-1, preferred, auto, 1

# Any unknown/new display — auto configure at preferred settings
monitor = , preferred, auto, 1
